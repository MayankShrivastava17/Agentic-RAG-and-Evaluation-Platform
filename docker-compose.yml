services:
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
    volumes:
      - qdrant_data:/qdrant/storage

  api:
    build: .
    env_file:
      - .env
    depends_on:
      - qdrant
    ports:
      - "8000:8000"
    volumes:
      - ./:/app

volumes:
  qdrant_data:
